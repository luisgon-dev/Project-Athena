services:
    book-router:
        build:
            context: .
            dockerfile: Dockerfile
        image: book-router:local
        container_name: book-router
        environment:
            EBOOKS_ROOT: /ebooks
            AUDIOBOOKS_ROOT: /audiobooks
            DATABASE_PATH: /data/book-router/book-router.sqlite
        ports:
            - "3001:3000"
        volumes:
            - ./data:/data/book-router
            - ./media/ebooks:/ebooks
            - ./media/audiobooks:/audiobooks
